// Path: index.html
//Make sure to include the script.js file in the index.html file

//make a function that returns an integer
function add(a, b) {
    return a + b;
    }

//make a main function
function main() {
    //print the result of the add function
    }

function formSubmit()
{
    //get the input from the user
    console.log("clicked");
    var input = document.getElementById('input').value;
    console.log(input);
    document.getElementById('output').innerHTML = input;
}